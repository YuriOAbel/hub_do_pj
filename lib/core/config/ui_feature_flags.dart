/// Temporary UI toggles — keep code, hide widgets when `false`.
abstract final class UiFeatureFlags {
  static const bool showHomeCarousel = false;
  static const bool showHomeFilterButton = false;
}
